# --- OS ---
.DS_Store
Thumbs.db

# --- Editors ---
.vscode/
.idea/
*.swp

# --- Node / frontend ---
node_modules/
dist/
coverage/
*.tsbuildinfo
.vite/

# --- Python / backend ---
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Environment ---
.env
.env.*
!.env.example
!.env.*.example

# --- Logs ---
*.log
logs/

# --- Build / misc ---
*.local

# Playwright MCP writes page snapshots and console logs into the working
# directory; they are run artefacts, not part of the project.
.playwright-mcp/

# Local MCP client configuration. It carries per-developer API keys
# (e.g. an x-api-key header), so it must never be committed.
.mcp.json
